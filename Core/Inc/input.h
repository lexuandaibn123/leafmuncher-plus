#ifndef INPUT_H
#define INPUT_H

/* input — đọc joystick analog (ADC1+DMA) + nút (PB7 JOY_SW, PA0 B1) → InputEvent.
 * Lớp driver — chạm HAL. Hiệu chỉnh center + deadzone + debounce. Hợp đồng: contracts/input.md.
 * Skeleton Phase 1 — hiện thực ở Phase 2 (T013–T015). */

#include "game.h"   /* InputEvent, Dir */

#endif /* INPUT_H */
